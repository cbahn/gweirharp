{
    "scripts": {
        "start": "eleventy --serve",
        "build": "eleventy",
        "watch-sass": "sass --watch --quiet sass:assets/css"
    },
    "devDependencies": {
        "@11ty/eleventy": "^3.1.2",
        "sass": "=1.62.1"
    }
}
